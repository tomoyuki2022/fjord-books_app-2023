<p style="color: green"><%= notice %></p>
<div class="show-item">
  <%= render @book %>
</div>
<nav class="page-nav">
  <%= link_to t('views.actions.edit'), edit_book_path(@book) %> |
  <%= link_to t('views.actions.back'), books_path %>
  <%= button_to t('views.actions.destroy'), @book, method: :delete %>
</nav>
